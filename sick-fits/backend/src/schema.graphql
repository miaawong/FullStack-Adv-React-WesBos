# import * from './generated/prisma.graphql'
# ^^ importing files in graphql

type Mutation {
    createItem(
        title: String
        description: String
        price: Int
        image: String
        largeImage: String
    ): Item!
}

type Query {
    items: [Item]!
}
